services:
  - type: web
    name: email-tracker
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn tracker:app --host 0.0.0.0 --port 10000
    envVars:
      - key: DATABASE_URL
        sync: false  # You’ll set this manually in the Render dashboard